<%# <%= if current_user.has_liked?%>
<%# <%= button_to 'Like Post', like_post_path(@post) %>
<%# <%= @post.likes.count%>
<%# <%byebug%>

<%# <%= button_to 'Unlike Post', unlike_post_path(@post.likes.ids.first) %>

<%# <%= button_to 'Unlike Post', {controller: "likes", action: "destroy", id: @post.likes.ids.first}, method: :delete, data: { confirm: 'Are you sure?' } %>
<%# <%= @post.likes.count%>
<%# <%= else %>
<%# <%= end %>
<%# <%byebug%>
    <%# <% if @user.likes.pluck(:user_id).include?(current_user.id) %> %>
      <%= button_to 'Unlike Post', {controller: "likes", action: "destroy", id: @post.likes.ids.first.to_i}, method: :delete %>
    <%# <% else %> %>
         <%= button_to 'Like Post', like_post_path(@post) %>
  <%# <%end%> %>
    <%= @post.likes.count%>

